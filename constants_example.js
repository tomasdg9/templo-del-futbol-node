// Renombrar este archivo como constants.js con las variables de entorno correspondientes.
const DB_USER = 'userdb';
const DB_HOST = 'localhost';
const DB_DATABASE = 'namedb';
const DB_PASSWORD = 'passdb';
const DB_PORT = 5432;

const MAIL_SERVICE = 'service';
const MAIL_USER = 'mail';
const MAIL_PASSWORD = 'passwordmail';

module.exports = {
  DB_USER,
  DB_HOST,
  DB_DATABASE,
  DB_PASSWORD,
  DB_PORT,
  MAIL_SERVICE,
  MAIL_USER,
  MAIL_PASSWORD
};